/// <summary>
/// 플레이어가 조작하는 히어로 캐릭터 클래스입니다.
/// プレイヤーが操作するヒーローキャラクタークラスです。
/// Hero character class controlled by the player.
/// </summary>
public class Hero : Character
{
    /// <summary>
    /// 히어로를 초기화합니다.
    /// ヒーローを初期化します。
    /// Initialize the hero.
    /// </summary>
    protected override void Initialize()
    {
        base.Initialize();
        // 추가 히어로 초기화 로직
        // 追加のヒーロー初期化ロジック
        // Additional hero initialization logic
    }
}
